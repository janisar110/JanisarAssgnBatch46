// create function and  create object store car info using 
function carInfo(manufacturer, model, color) {
    var carData = {
        manufacturer1: manufacturer,
        model1: model
    };
    return carData;
}
var CarInfo = carInfo("Toyota port qasim", "2022");
console.log(CarInfo);
